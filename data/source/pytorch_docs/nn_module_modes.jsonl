{"doc_id":"api_docs_module_train","title":"Module.train","url":"https://docs.pytorch.org/docs/stable/generated/torch.nn.Module.html#torch.nn.Module.train","source_type":"api_docs","section_path":["torch","nn","Module","train"],"text":"Module.train(mode=True) sets the module to training mode. This is a mode switch on nn.Module: it sets the module's training flag and propagates recursively to all submodules. The default mode when a module is created is training mode. The counterpart is model.eval(), which switches to evaluation mode. model.train() does not change module behavior beyond this flag, and it does not affect autograd or gradient tracking; to disable gradient computation, use torch.no_grad() separately. Only a few stateful layers read the training flag — nn.Dropout and nn.BatchNorm are the main ones, and each interprets it in its own independent way. For nn.Dropout, training mode turns the layer on so that it randomly zeros input elements during the forward pass. For nn.BatchNorm, training mode has a different effect entirely: the layer normalizes using statistics computed from the current mini-batch, and it also updates its stored running mean and running variance buffers. Dropout does not maintain running statistics of any kind; the running-statistics machinery is specific to batch normalization. These are two independent mechanisms that happen to share the same training flag. See torch.nn.Dropout and torch.nn.BatchNorm2d for each layer's own behavior. In a typical training loop, call model.train() before the training phase and model.eval() before the validation phase."}
{"doc_id":"api_docs_module_eval","title":"Module.eval","url":"https://docs.pytorch.org/docs/stable/generated/torch.nn.Module.html#torch.nn.Module.eval","source_type":"api_docs","section_path":["torch","nn","Module","eval"],"text":"Module.eval() sets the module to evaluation mode. This is equivalent to calling model.train(False). Evaluation mode is a flag on the module that a few stateful layers — nn.Dropout and nn.BatchNorm are the main ones — read individually, each in its own way. nn.Dropout is simply turned off: it passes all inputs through unchanged and does not zero any elements. For nn.BatchNorm the effect is completely different — the layer stops computing statistics from the current batch and instead normalizes using the running mean and running variance that were accumulated during training. Dropout has no running statistics of its own and does not do anything that resembles normalization; the running-statistics branch only exists in batch normalization. Calling model.eval() propagates evaluation mode to all submodules recursively. model.eval() does not disable gradient tracking or reduce memory usage. To disable gradient computation during inference, use torch.no_grad() as a separate step. The typical inference pattern is to call both model.eval() and wrap the forward pass in a torch.no_grad() context. model.eval() changes module behavior; torch.no_grad() controls gradient tracking."}
{"doc_id":"api_docs_nn_dropout","title":"nn.Dropout","url":"https://docs.pytorch.org/docs/stable/generated/torch.nn.Dropout.html","source_type":"api_docs","section_path":["torch","nn","Dropout"],"text":"torch.nn.Dropout(p=0.5, inplace=False) randomly zeroes some elements of the input tensor with probability p during training, using samples from a Bernoulli distribution. Each element is independently zeroed with probability p on every forward call. The remaining elements are scaled by 1/(1-p) to maintain the expected value of the output. This regularization technique helps prevent overfitting by forcing the network to not rely too heavily on any single neuron. During evaluation mode, dropout is completely disabled: all input elements are passed through unchanged with no scaling applied. The behavior is controlled by the module's training flag, which is set by model.train() and model.eval(). When the module is in training mode (model.train()), dropout is active. When in evaluation mode (model.eval()), dropout is disabled. This is why calling model.eval() before inference is important: if you forget, dropout will still randomly zero elements during prediction, producing inconsistent and degraded results. The inplace parameter controls whether the operation modifies the input tensor directly or creates a new tensor."}
{"doc_id":"api_docs_nn_batchnorm","title":"nn.BatchNorm2d","url":"https://docs.pytorch.org/docs/stable/generated/torch.nn.BatchNorm2d.html","source_type":"api_docs","section_path":["torch","nn","BatchNorm2d"],"text":"torch.nn.BatchNorm2d normalizes the input over the channel dimension using batch statistics during training and running statistics during evaluation. In training mode (model.train()), each forward pass computes the mean and variance from the current mini-batch, uses them to normalize the input, and updates the stored running mean and running variance using exponential moving average controlled by the momentum parameter. In evaluation mode (model.eval()), batch normalization uses the previously accumulated running statistics instead of computing statistics from the current input. This means the output is deterministic and does not depend on other samples in the batch. The running statistics are stored as running_mean and running_var buffers in the module. If the model is evaluated before sufficient training iterations, the running statistics may be inaccurate, leading to poor inference results. This is a common source of bugs: forgetting to call model.train() during training means running statistics are never updated, and the model will use incorrect statistics during evaluation. Similarly, forgetting model.eval() during inference means batch normalization will use mini-batch statistics, producing results that vary with batch composition. The key point is that batch normalization behaves differently in train mode and eval mode because of running statistics."}
{"doc_id":"api_docs_torch_no_grad","title":"torch.no_grad","url":"https://docs.pytorch.org/docs/stable/generated/torch.no_grad.html","source_type":"api_docs","section_path":["torch","no_grad"],"text":"torch.no_grad() is a context manager and decorator that disables gradient tracking for all operations inside its scope. Operations performed inside a no_grad block are not recorded by autograd, so no gradient computation graph is built for them. Computation results inside the block will generally not track gradients. This reduces memory consumption and speeds up computation during inference, because PyTorch does not need to store intermediate results for backpropagation. Note that no_grad does not change the requires_grad attribute of existing tensors that were created outside the block; it only prevents new operations from being tracked. The typical usage is: with torch.no_grad(): output = model(input). torch.no_grad() is fundamentally different from model.eval(). model.eval() changes module behavior: it disables dropout and switches batch normalization to use running statistics. torch.no_grad() controls gradient tracking: it prevents autograd from recording operations. They address different concerns and are not interchangeable. During inference, you typically want both: model.eval() to get correct module behavior, and torch.no_grad() to save memory and computation. During training, you want model.train() and do not use torch.no_grad(), because gradients are needed for backpropagation. A common mistake is assuming that model.eval() also disables gradients. It does not. Another common mistake is assuming that torch.no_grad() changes dropout or batch normalization behavior. It does not. Each one controls a separate axis: model.eval() controls module behavior, torch.no_grad() controls gradient tracking."}
