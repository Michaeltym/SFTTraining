{"input": "Does dropout use running statistics like batch normalization?", "output": "No, dropout has no running statistics at all. The only parameter stored on a dropout layer is the drop probability p. During training it zeros out a random fraction p of input elements and scales the rest by 1/(1-p); during evaluation it passes inputs through unchanged. Batch normalization is the layer that tracks running_mean and running_var during training and uses those buffers at eval time — dropout has no equivalent state."}
{"input": "After calling model.eval(), does dropout switch to using stored statistics instead of random zeroing?", "output": "It does not. model.eval() disables the random zeroing in dropout and nothing else: dropout becomes an identity passthrough that returns its input unchanged. There are no stored statistics to switch to — dropout never recorded any. The stored-statistics behavior at eval time belongs to batch normalization, which replaces current-batch statistics with running_mean and running_var once the module is in eval mode."}
{"input": "Is the difference between train mode and eval mode for dropout about which statistics it uses?", "output": "That framing is wrong for dropout. Dropout has no statistics, stored or otherwise. The only behavioral difference between train and eval for a Dropout layer is whether the random zeroing is active: in train mode it zeros a fraction p of elements and scales the survivors by 1/(1-p); in eval mode it returns its input unchanged. The statistics framing applies to batch normalization, which swaps between current-batch statistics in train mode and the running_mean / running_var buffers in eval mode."}
{"input": "Does torch.argmax(x, dim=1) return a scalar tensor?", "output": "No. Passing dim=1 tells argmax to reduce only along axis 1 and keep every other dimension, so the result has the same shape as x with axis 1 removed. If x has shape [B, N], argmax(x, dim=1) returns a LongTensor of shape [B]; if x has shape [B, N, C], it returns a LongTensor of shape [B, C]. A scalar 0-dim tensor only comes out of torch.argmax(x) with no dim argument, which flattens x first."}
{"input": "Does torch.argmax(x, dim=1) flatten the tensor before finding the maximum?", "output": "No. The whole point of passing dim=1 is that argmax reduces only along that single axis and does not flatten. For x of shape [B, N, C] and dim=1, argmax searches along the N axis independently at every (batch, channel) position, returning indices of shape [B, C]. Flattening only happens when you call torch.argmax(x) with no dim argument — that reduces over every axis and returns a single index into the flattened view."}
{"input": "What is the difference between torch.argmax(x) and torch.argmax(x, dim=1)?", "output": "torch.argmax(x) with no dim argument flattens x internally and returns a 0-dim LongTensor: a single index into the flattened view. torch.argmax(x, dim=1) reduces only along axis 1 and keeps every other dimension, so for x of shape [B, N, C] it returns a LongTensor of shape [B, C]. The two calls answer different questions: the first asks for the global argmax, the second asks for the argmax along axis 1 at each position of the remaining axes."}
